# 内核钉在 <7.1,保证 zfs-kmod 编得进。
# OpenZFS 2.4.x 的 Linux-Maximum=7.0:内核 >=7.1 会让 zfs-kmod configure 直接拒编,再被 @world 的
# --keep-going 静默跳过 → 出锅只剩 zfs 用户态、没有 zfs.ko,modprobe zfs 失败、根本装不了 ZFS
# (7.1.3 实机踩过这个坑)。钉在 <7.1(现最新 6.18.x,仍 ≤7.0 兼容,硬件支持也够新)。
# 树里已无 7.0.x,故不是"降到 7.0"而是"卡在 6.x 最新"。
# OpenZFS 正式支持 7.1(或树里出支持 7.1 的 zfs)后,删掉本文件即可跟回最新内核。
# 出锅前 99-sanitize 会硬断言 zfs.ko 真的在位,双保险防再次静默出不可装 ZFS 的盘。
>=sys-kernel/gentoo-kernel-bin-7.1
>=sys-kernel/gentoo-kernel-7.1
>=sys-kernel/gentoo-sources-7.1
